# Declarative TUI Library (C / libxml2/ ncurses)

This document describes the core model for a declarative, XML-driven TUI framework built on top of **ncurses**.
The goal is to define UI structure via XML and render it using a small, explicit library written in C.

# Workflow

